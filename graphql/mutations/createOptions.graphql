mutation CreateStoreOptions($input: StoreOptionsInput) {
  createStoreOptions(input: $input) {
    id
    slider
    slider_image
    bestProducts
    ourBrands
    whatsapp
    popup
    primaryColor
    bgColor
    popupImage
    storeId
  }
}